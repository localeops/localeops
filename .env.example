# =============================================================================
# LocaleOps Environment Variables
# =============================================================================
# Copy this file to set up your local environment:
#   cp .env.example .env
#
# Then fill in your actual values in the .env file and load it:
#   source .env
#
# Note: .env should be added to .gitignore (never commit secrets!)
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Adapter
# -----------------------------------------------------------------------------
# Generate at: https://github.com/settings/tokens
# Required permissions:
#   - Classic tokens: 'repo', 'pull_request'
#   - Fine-grained tokens: 'Contents' (read/write), 'Pull requests' (read/write)
GITHUB_ACCESS_TOKEN=ghp_your_token_here

# -----------------------------------------------------------------------------
# GitLab Adapter
# -----------------------------------------------------------------------------
# Uncomment and set if using GitLab:
# GITLAB_ACCESS_TOKEN=glpat_your_token_here

# -----------------------------------------------------------------------------
# Bitbucket Adapter
# -----------------------------------------------------------------------------
# Uncomment and set if using Bitbucket:
# BITBUCKET_ACCESS_TOKEN=your_token_here