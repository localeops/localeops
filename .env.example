# - All variables are REQUIRED for the app to start.

# Server settings
# PORT: Port for the HTTP server
PORT="3000"

# Locale source settings
# SOURCE_LOCALE: Base locale folder name you author in (e.g., "en")
# SOURCE_BRANCH: Git branch to read source content from and open translation PRs against
# SOURCE_DIRECTORY: Relative path (from repo root) to the locales root directory
SOURCE_LOCALE="en"
SOURCE_BRANCH="main"
SOURCE_DIRECTORY="src/locales"

# GitHub repository settings
# GITHUB_OWNER: Org or username that owns the repository (e.g., "acme-inc")
# GITHUB_REPO: Repository name (e.g., "webapp")
# GITHUB_TOKEN: Token with access to read/write contents and create PRs.
#   - Recommended scopes:
#     - For classic tokens: repo (full), pull_request
#     - For fine-grained: repository contents read/write, pull requests read/write
GITHUB_OWNER="your-org-or-username"
GITHUB_REPO="your-repo-name"
GITHUB_TOKEN="ghp_your-token"