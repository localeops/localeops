{
	"name": "docs",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"start": "astro dev",
		"build": "bun run generate:github-actions && bun run generate:class-docs && astro build",
		"generate:github-actions": "bun run scripts/generate-github-actions.ts",
		"generate:class-docs": "npx typedoc && bun run scripts/generate-class-docs.ts",
		"preview": "astro preview",
		"astro": "astro"
	},
	"dependencies": {
		"@astrojs/starlight": "^0.37.1",
		"astro": "^5.16.11",
		"sharp": "^0.34.2"
	},
	"devDependencies": {
		"typedoc-plugin-markdown": "^4.9.0"
	}
}
